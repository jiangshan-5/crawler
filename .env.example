# 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# API Keys
API_KEY=your_api_key_here

# Database (如果需要)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=crawler_db
DB_USER=your_username
DB_PASSWORD=your_password

# Proxy (如果需要)
HTTP_PROXY=
HTTPS_PROXY=
